gymnasium>=0.29.0
numpy>=1.24.0
torch>=2.0.0
pyyaml>=6.0
wandb>=0.15.0          # optional — set use_wandb: false in config.yaml to skip

# MuJoCo physics backend (recommended — smoother, more controllable)
mujoco>=3.0.0
dm_control>=1.0.0
absl-py                # required by dm_control
dm_env                 # required by dm_control
dm-tree                # required by dm_control
glfw                   # required by mujoco.viewer (GUI rendering)

# PyBullet physics backend (legacy — requires gym-pybullet-drones)
# gym-pybullet-drones installs pybullet automatically
# pip install git+https://github.com/utiasDSL/gym-pybullet-drones.git
